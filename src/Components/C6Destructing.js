import React from 'react'

const C6Destructing = ({name,heroName}) => {
  return (
    <div>This is array destructuring.
        Hello {name} aka {heroName}
    </div>
  )
}

export default C6Destructing